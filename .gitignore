# Laravel / PHP
/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor

# Environment / local configuration
.env
.env.backup
.env.production
/auth.json

# IDE / editor
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Scratch / FUSE mount artifacts (Cowork device bridge)
_to_delete/
.fuse_hidden*
/individual

# Developer licence toggle
# Machine-bound, never committed, never shipped
/licence-off.key

# Local licence
# Machine-bound, never committed, never shipped
/license.lic

# Local Windows shortcuts
# Uncomment if .lnk files are only developer-local
# *.lnk
